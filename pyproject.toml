[project]
name = "tool-browser"
version = "0.2.0"
description = "Headless browser automation tool for Kiso"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.44",
]

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv]
managed = true

[tool.pytest.ini_options]
pythonpath = ["."]
